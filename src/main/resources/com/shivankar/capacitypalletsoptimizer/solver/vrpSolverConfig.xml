<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://www.optaplanner.org/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://www.optaplanner.org/xsd/solver https://www.optaplanner.org/xsd/solver/solver.xsd">

  <!-- Domain model configuration -->
  <solutionClass>com.shivankar.capacitypalletsoptimizer.model.VrpSolution</solutionClass>
  <entityClass>com.shivankar.capacitypalletsoptimizer.model.Vehicle</entityClass>

  <!-- Score configuration -->
  <scoreDirectorFactory>
    <constraintProviderClass>com.shivankar.capacitypalletsoptimizer.solver.VrpConstraintProvider</constraintProviderClass>
  </scoreDirectorFactory>

  <!-- Optimization algorithm configuration -->
  <termination>
    <secondsSpentLimit>30</secondsSpentLimit>
    <unimprovedSecondsSpentLimit>10</unimprovedSecondsSpentLimit>
  </termination>

  <!-- Construction heuristic -->
  <constructionHeuristic>
    <constructionHeuristicType>FIRST_FIT</constructionHeuristicType>
  </constructionHeuristic>

  <!-- Local search -->
  <localSearch>
    <unionMoveSelector>
      <listChangeMoveSelector/>
      <listSwapMoveSelector/>
      <subListChangeMoveSelector/>
      <subListSwapMoveSelector/>
    </unionMoveSelector>
    <acceptor>
      <lateAcceptanceSize>400</lateAcceptanceSize>
    </acceptor>
    <forager>
      <acceptedCountLimit>1</acceptedCountLimit>
    </forager>
  </localSearch>

</solver>